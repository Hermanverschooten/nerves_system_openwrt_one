# Set the device serial number at burn time. This is read at runtime by
# Nerves.Runtime.KV. Empty by default; defaults to a built-in chip ID.
uboot_setenv(uboot-env, "nerves_serial_number", "\${NERVES_SERIAL_NUMBER}")
